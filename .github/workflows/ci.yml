name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  rust-matrix:
    name: Rust ${{ matrix.os }} (fmt · clippy · test)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Record environment
        run: |
          rustc --version > rust-env.txt
          cargo --version >> rust-env.txt
          echo "target=${{ matrix.os }}" >> rust-env.txt
      - name: Format
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Unit tests
        run: cargo test --workspace --all-features
      - name: Upload environment evidence
        uses: actions/upload-artifact@v4
        with:
          name: rust-env-${{ matrix.os }}
          path: rust-env.txt

  js-matrix:
    name: JS/TS ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install
      - run: pnpm --filter @helixforge/console typecheck
      - run: pnpm --filter @helixforge/helix-code-web typecheck
      - run: pnpm --filter @helixforge/helix-collab-web typecheck
      - run: pnpm --filter @helixforge/console build
      - run: pnpm --filter @helixforge/helix-collab-web build

  security:
    name: Security scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked || true
      - name: cargo audit
        run: cargo audit || true
      - name: Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  docker-compose-config:
    name: Compose config validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker compose config -q

  docker-image:
    name: Docker image build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build helix-core image
        run: docker build -t helixforge/helix-core:ci .
        # Full multi-stage Rust build — may take 15–30m; required for sovereign deploy path

  helm-lint:
    name: Helm chart lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
      - name: helm lint
        run: |
          helm lint infra/helm/helix-core \
            --set secrets.databaseUrl=postgres://ci:ci@db/helix \
            --set secrets.vaultMasterKey=ci-vault-master-key-not-prod

  helm-template:
    name: Helm template render
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
      - name: helm template prod
        run: |
          helm template helix-core-prod infra/helm/helix-core \
            -f infra/helm/helix-core/values.yaml \
            -f infra/helm/helix-core/values-prod.yaml \
            --set secrets.databaseUrl=postgres://ci:ci@db/helix \
            --set secrets.vaultMasterKey=ci-vault-master-key-not-prod

  terraform-validate:
    name: Terraform validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - name: terraform init & validate
        run: |
          for env in dev prod; do
            echo "=== validate $env ==="
            terraform -chdir=infra/terraform/environments/$env init -backend=false
            terraform -chdir=infra/terraform/environments/$env validate
          done

  smoke:
    name: Core smoke (docker compose)
    runs-on: ubuntu-latest
    needs: docker-image
    steps:
      - uses: actions/checkout@v4
      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init
      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1
      - name: Build helix-core image
        run: docker build -t helixforge/helix-core:smoke .
      - name: Apply migrations
        run: |
          docker run --rm --network host \
            -e DATABASE_URL=postgres://helix:helix@127.0.0.1:55432/helixforge \
            helixforge/helix-core:smoke helix-migrate
      - name: Start gateway
        run: |
          docker run -d --name helix-gateway-smoke \
            --network host \
            -e HELIX_ENV=local \
            -e HELIX_LOCAL_DEV_UNSAFE=1 \
            -e HELIX_ALLOW_DEV_HEADERS=1 \
            -e DATABASE_URL=postgres://helix:helix@127.0.0.1:55432/helixforge \
            -e NATS_URL=nats://127.0.0.1:4222 \
            -e MINIO_ENDPOINT=http://127.0.0.1:9000 \
            -e HELIX_VAULT_MASTER_KEY=smoke-smoke-smoke-master-key \
            helixforge/helix-core:smoke gateway
          for i in $(seq 1 30); do
            curl -fs http://127.0.0.1:8080/healthz && exit 0
            sleep 2
          done
          docker logs helix-gateway-smoke
          exit 1

  collab-smoke:
    name: HelixCollab smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install
      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init
      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1
      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
      - name: Build HelixCollab API
        run: cargo build -p helix_collab_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1
      - name: Run HelixCollab integration tests
        run: cargo test -p helix_collab_api -- --ignored
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1
      - name: Start HelixCollab API
        run: |
          ./target/debug/helix_collab_api > collab-api.log 2>&1 &
          echo "COLLAB_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8101/healthz && exit 0
            sleep 2
          done
          cat collab-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1
      - name: Run HelixCollab deep smoke
        shell: pwsh
        run: pwsh -File scripts/helix_collab_smoke.ps1
      - name: Stop HelixCollab API
        if: always()
        run: kill $COLLAB_PID || true

  insights-smoke:
    name: HelixInsights smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixInsights API
        run: cargo build -p helix_insights_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixInsights API
        run: |
          ./target/debug/helix_insights_api > insights-api.log 2>&1 &
          echo "INSIGHTS_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8104/healthz && exit 0
            sleep 2
          done
          cat insights-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixInsights smoke
        shell: pwsh
        run: pwsh -File scripts/helix_insights_smoke.ps1

      - name: Stop HelixInsights API
        if: always()
        run: kill $INSIGHTS_PID || true

  commerce-smoke:
    name: HelixCommerce smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixCommerce API
        run: cargo build -p helix_commerce_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixCommerce API
        run: |
          ./target/debug/helix_commerce_api > commerce-api.log 2>&1 &
          echo "COMMERCE_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8105/healthz && exit 0
            sleep 2
          done
          cat commerce-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixCommerce smoke
        shell: pwsh
        run: pwsh -File scripts/helix_commerce_smoke.ps1

      - name: Stop HelixCommerce API
        if: always()
        run: kill $COMMERCE_PID || true

  edu-smoke:
    name: HelixEdu smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixEdu API
        run: cargo build -p helix_edu_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixEdu API
        run: |
          ./target/debug/helix_edu_api > edu-api.log 2>&1 &
          echo "EDU_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8106/healthz && exit 0
            sleep 2
          done
          cat edu-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixEdu smoke
        shell: pwsh
        run: pwsh -File scripts/helix_edu_smoke.ps1

      - name: Stop HelixEdu API
        if: always()
        run: kill $EDU_PID || true

  capital-smoke:
    name: HelixCapital smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixCapital API
        run: cargo build -p helix_capital_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixCapital API
        run: |
          ./target/debug/helix_capital_api > capital-api.log 2>&1 &
          echo "CAPITAL_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8107/healthz && exit 0
            sleep 2
          done
          cat capital-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixCapital smoke
        shell: pwsh
        run: pwsh -File scripts/helix_capital_smoke.ps1

      - name: Stop HelixCapital API
        if: always()
        run: kill $CAPITAL_PID || true

  well-smoke:
    name: HelixWell smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixWell API
        run: cargo build -p helix_well_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixWell API
        run: |
          ./target/debug/helix_well_api > well-api.log 2>&1 &
          echo "WELL_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8108/healthz && exit 0
            sleep 2
          done
          cat well-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixWell smoke
        shell: pwsh
        run: pwsh -File scripts/helix_well_smoke.ps1

      - name: Stop HelixWell API
        if: always()
        run: kill $WELL_PID || true

  network-smoke:
    name: HelixNetwork smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixNetwork API
        run: cargo build -p helix_network_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixNetwork API
        run: |
          ./target/debug/helix_network_api > network-api.log 2>&1 &
          echo "NETWORK_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8109/healthz && exit 0
            sleep 2
          done
          cat network-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixNetwork smoke
        shell: pwsh
        run: pwsh -File scripts/helix_network_smoke.ps1

      - name: Stop HelixNetwork API
        if: always()
        run: kill $NETWORK_PID || true

  forge-studio-smoke:
    name: HelixForge Studio smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixForge Studio API
        run: cargo build -p helix_forge_studio_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixForge Studio API
        run: |
          ./target/debug/helix_forge_studio_api > forge-studio-api.log 2>&1 &
          echo "STUDIO_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8110/healthz && exit 0
            sleep 2
          done
          cat forge-studio-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixForge Studio smoke
        shell: pwsh
        run: pwsh -File scripts/helix_forge_studio_smoke.ps1

      - name: Stop HelixForge Studio API
        if: always()
        run: kill $STUDIO_PID || true

  synthbio-smoke:
    name: HelixSynthBio smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixSynthBio API
        run: cargo build -p helix_synthbio_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixSynthBio API
        run: |
          ./target/debug/helix_synthbio_api > synthbio-api.log 2>&1 &
          echo "SYNTHBIO_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8111/healthz && exit 0
            sleep 2
          done
          cat synthbio-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixSynthBio smoke
        shell: pwsh
        run: pwsh -File scripts/helix_synthbio_smoke.ps1

      - name: Stop HelixSynthBio API
        if: always()
        run: kill $SYNTHBIO_PID || true

  lex-prime-smoke:
    name: HelixLex Prime smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixLex Prime API
        run: cargo build -p helix_lex_prime_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixLex Prime API
        run: |
          ./target/debug/helix_lex_prime_api > lex-prime-api.log 2>&1 &
          echo "LEX_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8112/healthz && exit 0
            sleep 2
          done
          cat lex-prime-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixLex Prime smoke
        shell: pwsh
        run: pwsh -File scripts/helix_lex_prime_smoke.ps1

      - name: Stop HelixLex Prime API
        if: always()
        run: kill $LEX_PID || true

  cura-prime-smoke:
    name: HelixCura Prime smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixCura Prime API
        run: cargo build -p helix_cura_prime_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixCura Prime API
        run: |
          ./target/debug/helix_cura_prime_api > cura-prime-api.log 2>&1 &
          echo "CURA_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8113/healthz && exit 0
            sleep 2
          done
          cat cura-prime-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixCura Prime smoke
        shell: pwsh
        run: pwsh -File scripts/helix_cura_prime_smoke.ps1

      - name: Stop HelixCura Prime API
        if: always()
        run: kill $CURA_PID || true

  terra-prime-smoke:
    name: HelixTerra Prime smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixTerra Prime API
        run: cargo build -p helix_terra_prime_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixTerra Prime API
        run: |
          ./target/debug/helix_terra_prime_api > terra-prime-api.log 2>&1 &
          echo "TERRA_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8114/healthz && exit 0
            sleep 2
          done
          cat terra-prime-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixTerra Prime smoke
        shell: pwsh
        run: pwsh -File scripts/helix_terra_prime_smoke.ps1

      - name: Stop HelixTerra Prime API
        if: always()
        run: kill $TERRA_PID || true

  climate-prime-smoke:
    name: HelixClimate Prime smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixClimate Prime API
        run: cargo build -p helix_climate_prime_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixClimate Prime API
        run: |
          ./target/debug/helix_climate_prime_api > climate-prime-api.log 2>&1 &
          echo "CLIMATE_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8115/healthz && exit 0
            sleep 2
          done
          cat climate-prime-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixClimate Prime smoke
        shell: pwsh
        run: pwsh -File scripts/helix_climate_prime_smoke.ps1

      - name: Stop HelixClimate Prime API
        if: always()
        run: kill $CLIMATE_PID || true

  orbit-prime-smoke:
    name: HelixOrbit Prime smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixOrbit Prime API
        run: cargo build -p helix_orbit_prime_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixOrbit Prime API
        run: |
          ./target/debug/helix_orbit_prime_api > orbit-prime-api.log 2>&1 &
          echo "ORBIT_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8116/healthz && exit 0
            sleep 2
          done
          cat orbit-prime-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixOrbit Prime smoke
        shell: pwsh
        run: pwsh -File scripts/helix_orbit_prime_smoke.ps1

      - name: Stop HelixOrbit Prime API
        if: always()
        run: kill $ORBIT_PID || true

  quantum-forge-smoke:
    name: HelixQuantum Forge smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixQuantum Forge API
        run: cargo build -p helix_quantum_forge_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixQuantum Forge API
        run: |
          ./target/debug/helix_quantum_forge_api > quantum-forge-api.log 2>&1 &
          echo "QUANTUM_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8117/healthz && exit 0
            sleep 2
          done
          cat quantum-forge-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixQuantum Forge smoke
        shell: pwsh
        run: pwsh -File scripts/helix_quantum_forge_smoke.ps1

      - name: Stop HelixQuantum Forge API
        if: always()
        run: kill $QUANTUM_PID || true

  vita-prime-smoke:
    name: HelixVita Prime smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixVita Prime API
        run: cargo build -p helix_vita_prime_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixVita Prime API
        run: |
          ./target/debug/helix_vita_prime_api > vita-prime-api.log 2>&1 &
          echo "VITA_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8118/healthz && exit 0
            sleep 2
          done
          cat vita-prime-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixVita Prime smoke
        shell: pwsh
        run: pwsh -File scripts/helix_vita_prime_smoke.ps1

      - name: Stop HelixVita Prime API
        if: always()
        run: kill $VITA_PID || true

  grid-prime-smoke:
    name: HelixGrid Prime smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixGrid Prime API
        run: cargo build -p helix_grid_prime_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixGrid Prime API
        run: |
          ./target/debug/helix_grid_prime_api > grid-prime-api.log 2>&1 &
          echo "GRID_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8119/healthz && exit 0
            sleep 2
          done
          cat grid-prime-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixGrid Prime smoke
        shell: pwsh
        run: pwsh -File scripts/helix_grid_prime_smoke.ps1

      - name: Stop HelixGrid Prime API
        if: always()
        run: kill $GRID_PID || true

  nova-labs-smoke:
    name: HelixNova Labs smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixNova Labs API
        run: cargo build -p helix_nova_labs_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixNova Labs API
        run: |
          ./target/debug/helix_nova_labs_api > nova-labs-api.log 2>&1 &
          echo "NOVA_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8120/healthz && exit 0
            sleep 2
          done
          cat nova-labs-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixNova Labs smoke
        shell: pwsh
        run: pwsh -File scripts/helix_nova_labs_smoke.ps1

      - name: Stop HelixNova Labs API
        if: always()
        run: kill $NOVA_PID || true

  pulse-smoke:
    name: HelixPulse smoke
    runs-on: ubuntu-latest
    needs: rust-matrix
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile || pnpm install

      - name: Start core infrastructure
        run: docker compose up -d postgres nats minio minio-init

      - name: Wait for Postgres
        run: |
          for i in $(seq 1 30); do
            docker compose exec -T postgres pg_isready -U helix -d helixforge && exit 0
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: cargo run -p helix_db --bin helix-migrate
        env:
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge

      - name: Build HelixPulse API
        run: cargo build -p helix_pulse_api
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Start HelixPulse API
        run: |
          ./target/debug/helix_pulse_api > pulse-api.log 2>&1 &
          echo "PULSE_PID=$!" >> $GITHUB_ENV
          for i in $(seq 1 60); do
            curl -fs http://127.0.0.1:8121/healthz && exit 0
            sleep 2
          done
          cat pulse-api.log
          exit 1
        env:
          HELIX_ENV: local
          HELIX_LOCAL_DEV_UNSAFE: 1
          HELIX_ALLOW_DEV_HEADERS: 1
          HELIX_DEV_PLATFORM: 1
          DATABASE_URL: postgres://helix:helix@127.0.0.1:55432/helixforge
          NATS_URL: nats://127.0.0.1:4222
          MINIO_ENDPOINT: http://127.0.0.1:9000
          HELIX_VAULT_MASTER_KEY: smoke-smoke-smoke-master-key
          HELIX_AUDIT_HMAC_SECRET: smoke-audit-hmac-secret
          HELIX_WEBHOOK_ALLOW_UNSIGNED: 1

      - name: Run HelixPulse smoke
        shell: pwsh
        run: pwsh -File scripts/helix_pulse_smoke.ps1

      - name: Stop HelixPulse API
        if: always()
        run: kill $PULSE_PID || true
